define(function (require) {

    var language = 'ch';
    var data = {
        ch: {
            pager: {
                previousPage: '上一页',
                nextPage: '下一页'
            }
        }
    };

    return data[language];
});